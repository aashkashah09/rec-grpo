"""Router: context featurizer, bandit policies, composite reward, propensity logger.

Implemented in Phase 2. Policies and reward are kept as pure functions so they are
trivially testable (see ``CLAUDE.md`` code conventions).
"""
