"""Environment: seeded SQLite DB, task templates, sandboxed tools, episode loop, verifier.

Implemented in Phase 1. Houses the deterministic task ground truth and value-comparison
verifier that anchor the project's honesty guarantees (see ``PROJECT_PLAN.md`` §1.3).
"""
