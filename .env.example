# Copy to .env and fill in. .env is gitignored; never commit real keys.
# Phase 0 ships key NAMES only — no values, no secrets.

# Frontier API provider (choose one; api_agent is provider-agnostic). Phase 2+.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Hugging Face (model/tokenizer downloads). Phase 3.
HF_TOKEN=

# Weights & Biases (training/experiment logging). Phase 3.
WANDB_API_KEY=
